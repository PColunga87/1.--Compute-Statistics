Se realizan 2 archivos .py, debido a que se cuentan con valores negativos en los datos de muestra, al sumarlo dan el mismo resultado que el ejemplo compartido por el Doctor en el archivo "Results", 
entonces agregué valores absolutos por si hubiera algún error de dedo en cuanto a los valores negativos.

Pablo Colunga